apiVersion: v1
kind: Secret
metadata:
  name: visitor-counter-secret
type: Opaque
stringData:
  DB_HOST: visitor-counter-postgres.postgres.database.azure.com
  DB_USER: postgresadmin
  # NOTE: Password should be set from Azure Key Vault in production
  # Temporarily using deployment default - replace with secure value
  DB_PASSWORD: TempPassword123!